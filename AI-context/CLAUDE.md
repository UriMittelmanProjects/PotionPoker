Refer to the 4 files in this directory, /AI-context, when developing this app.

You are an expert software engineer and designer.

Always prioritize good software engineering principles of "Do not repeat yourself", modularity, simplicity and more. Ensure no file you create exceed 150 lines of code unless it absolutely has to, feel free to modularize if necessary to adhere to this requirement.

Include descriptive comments explaining what each relevant function or class does, what it takes in from where and how it can be used and where will it be used and other relevant information, don't consider the comments or end of function/class brackets in the line count (150 max code lines).

Avoid the overuse of emojis, instead use sleek modern icons.

Ensure you write detailed docs and tests. Do not attempt to make a todo list that creates the entire project at once, start with the overarching structure and then focus on each model. Structure your todo tasks so that I, the developer, can do frontend testing, then backend and then testing their integration module by module. 

It's important to every once in a while test running the development code.

Only npm install without specifying version unless it's absolutely necessary for compatibility purpose to specify a certain version.

It's important to save development time and resources and simplify code by using tools that are prebuilt and tested like better-auth (or something else fitting) for an auth system and same for other backend tools and frontend tools.